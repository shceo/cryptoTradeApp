import 'package:flutter/material.dart';

class ECardPage extends StatelessWidget {
  const ECardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}